<!--
Purpose:        Bind PR #15 review remediation to regression and verification evidence
Owner:          Implementer / Tester / Reviewer
Update Trigger: Review findings or verified remediation changes
Harness Version: 1.1
-->

# PR #15 Remediation — 2026-09-06

Base: 3230bc4a40f6394381661d707b3d08d3b636706b.
User authorized review, implementation, commit and push. Five external review findings
were verified against source before changes. No external review replies were sent.

## Findings and changes

| Finding | Change | Exact regression test |
|---|---|---|
| R1 / 3943400835: stale coverage lacks warning | Shared clock, existing validated Seoul age, >=7 display warning; synthetic disclaimer distinct | `warns at Seoul age seven and beyond at %s`; `labels stale synthetic coverage without implying a failed production refresh` |
| R1: open-page clock changes | Refresh at Seoul midnight and on focus/visibility; cleanup timer/listeners | `refreshes an open page exactly at Seoul midnight and cleans up its timer`; `refreshes on focus and visibility after clock or background changes` |
| R1: unknown or invalid evidence | Unavailable, impossible and future dates display unknown | `never presents unknown or rejected coverage %s as fresh` |
| R2 / 3943400837: equal counts silence live region | Ephemeral submission sequence changes text in a stable live region on every valid submit | `announces every submission even when counts and query repeat` |
| R3 / 3943400839: error survives editing | Clear invalid submission on typing/example, retain valid results while editing | `clears an obsolete invalid error on %s without submitting`; existing `keeps submitted context distinct from the draft and replaces results after another search` |
| R4 / 3943400841: blank name hides identity | Explicit missing-value heading/article label; original nonblank name preserved | `gives missing-name address matches a visible and accessible fallback` |
| R5 / 3943400844: page lacks dataset provenance | Required internal dataset sourceLabel/sourceUrl shown outside cards via existing SourceLink | `keeps exact dataset provenance accessible before search and with no matches or invalid input` |

Browser regression: `updates stale demo evidence at Seoul midnight and announces equal-count
repeated searches` runs on Chromium, Firefox, WebKit and mobile Chromium. It proves changing
1/0-count announcements, identical resubmission, stale page/card warnings and example-based
error recovery. `has no WCAG violations with stale coverage warnings` runs desktop/mobile.

## Boundary and scope

Current user-provided constitution explicitly says Seoul age >=7 (ADR-015), superseding
the older merged >7 language cited by the reviewer. AGENTS.md is aligned with that instruction.
The historical V1 pipeline helper remains unchanged: UI uses its validated ageDays and applies
the current display boundary. Synthetic warnings never claim a failed real data refresh.
No source dates are inferred from retrieval or row modification timestamps.

Dataset-level provenance is internal presentation metadata, not public JSON or a new source
service. Query sequence and clock remain transient browser state. No dependencies, status
mapping, workflow, provider collection, deployment or handbook work changed.
TASK-015 still owns broader loading/recovery UX; production and release gates remain open.

## Verification

- Test-first RED: missing clock module and five App regressions failed as expected
  against the pre-fix implementation (/tmp/pr15-red.log).
- Component GREEN: 38 tests passed; no tests skipped.
- Fixed test callback return types identified by TypeScript; no behavior changed for this correction.
- Pinned Node 24.19.0/npm 11.17.0 `npm run verify:full`: exit 0, 492 tests, 24 browser tests,
  8 zero-violation accessibility scans. Log: /tmp/pr15-remediation-full.log.
- Global coverage: 92.85% statements / 92.20% branches / 96.37% functions / 95.07% lines.
- Existing synthetic/source quality gates passed unchanged. No production performance conclusion.
- git diff --check: passed. Runtime/source/test manifest: reports/pr15-verification-manifest.json.

## Independent re-review

Verdict: Approved. Independent review_task014 agent inspected all remediation changes,
confirmed all five findings resolved, and reran 38 component tests, typecheck and whitespace
checks. It reviewed the full verification log and found no remaining issue. The reviewer
confirmed no status mapping, historical helper, dependency, public-schema or deployment change.
