# TASK-009/010 collection-date publication verification

Date: 2026-09-12. Branch: `codex/task-009-010-publication`, base `ec6bb6f`.
Scope: local implementation and self-review, not independent approval or hosted deployment.

## Result

Pinned Node 24.19.0 / npm 11.17.0 `npm run verify` passed: 636 tests, two existing Windows
skips, successful lint/format/typecheck, coverage, Vite build and both search-quality checks.
Coverage: statements 91.30%, branches 91.18%, functions 95.23%, lines 93.16%; the unchanged
status mapper's 100% file gate also passed. `npm run test:a11y` passed all 20 tests.

`npm run verify:full` remains unsuccessful: the four-browser suite passed 66/68 cases but
Windows WebKit failed `opens candidate-only map searches by keyboard without automatic
provider I/O` and `TASK-017 reaches source and map links in order and activates source only
on request`. Both failures reproduced on a separate detached **unchanged `ec6bb6f` checkout**
using the same pinned runtime and browser installation. Do not claim full-suite completion.
An experimental Windows Alt+Tab adjustment did not resolve them and was reverted; the E2E
test file and product keyboard behavior are unchanged.

The first run with ambient Node 24.14.1 also exposed the existing pinned-Unicode runtime
assertion and Windows-only CLI path/symlink failures. Downloaded the already approved Node
24.19.0 distribution to temporary storage and verified its SHA-256 against the official
distribution checksums. Its bundled npm is 11.17.0. Corrected only test harness paths using
`fileURLToPath` and Windows junctions in `search-quality.test.ts`; all eight affected CLI
checks pass under the pinned runtime. No package pins or dependencies changed.

## Requirements and concrete evidence

| Requirement | Test/evidence |
|---|---|
| Keep real-data criteria deferred; do not invent policy/baseline | `does not waive quality policy or initial baseline review for collection dates`; publication/config.json intentionally absent |
| Use collection date without asserting source coverage | `uses Seoul collection date while retaining unverified source coverage`; `labels collection dates and warns without claiming source coverage at %s` |
| Warn at seven Seoul calendar days | `warns after seven Seoul collection days at %s`; collection UI boundary cases immediately before/at Seoul midnight |
| Prevent mixed date evidence and regression | `rejects mixing coverage and collection baselines`; `rejects asserted coverage in collection mode and regressing collection dates` |
| Preserve complete literal source rows | `preserves multiline quoted fields, escaped quotes, Korean and final empty cells`; malformed quoting/header/field-count/encoding and row-budget tests in parse-license-csv.test.ts |
| FR-13 exact-byte publication and matching baseline | `binds exact dataset bytes and matching baseline without asserting source data date`; `refuses a total artifact larger than the explicit JSON budget` |
| FR-13 preserve existing release and remove incomplete staging | `promotes a complete staged directory and preserves an existing release on retry or rejection`; `a competing publication lock prevents promotion and removes incomplete staging`; `a partial staging write failure preserves known-good bytes and cleans the candidate` |
| Build integration must use collection data, not the demo | `builds real collection-date assets and rejects tampered staging before building` exercises actual Vite invocation, emitted asset, baseline bytes and rejection before output creation |
| FR-12 complete browser snapshot and query-independent I/O | `loads the complete collection-date snapshot from a fixed credential-free asset URL`; three incomplete/mislabeled cases and aborted/HTTP failure case in publication-loader.test.tsx |
| Restore only state belonging to the deployed release | `reads only a baseline bound to an unchanged deployed release without credentials or redirects`; `rejects %s deployed state instead of bootstrapping` for missing/oversized/syntax/hash/changed/metadata failures |
| Daily/manual trusted preparation and guarded Pages publication | .github/workflows/refresh.yml; read-only verification in verify.yml; local YAML checks and separate Actions self-review |

The Vite integration test initially detected that plugin resolution left the demo loader in
place. The publication plugin now runs with `enforce: pre`; the passing test proves a collected
asset is emitted, no demo parts are emitted, matching baseline bytes accompany the build, and
tampered staging is rejected. The build writes the already verified in-memory state bytes,
avoiding a second unverified file read while packaging.

The write-failure test injects a failure after the first file is written using Node's mutable
filesystem export and synchronized ESM bindings. It asserts candidate cleanup and unchanged
known-good bytes, then restores the bindings. No production test hook was added.

## Workflow verification

Parsed both YAML documents with the YAML parser already bundled in the installed Playwright
tooling (`playwright-core/lib/utilsBundle.js`); no dependency was added. Duplicate-key parsing,
trigger/job presence, deny-all defaults and 40-character top-level action pins pass. This is
syntax and bounded structural verification, not GitHub-hosted semantic validation.

The official action tag references were checked using `git ls-remote`:

| Action version | Resolved commit |
|---|---|
| actions/checkout v5.0.0 | 08c6903cd8c0fde910a37f88322edcfb5dd907a8 |
| actions/setup-node v5.0.0 | a0853c24544627f65ddf259abe73b1d18a591444 |
| actions/upload-pages-artifact v3.0.1 | 56afc609e74202658d3ffba0e8f6dda462b719fa |
| actions/deploy-pages v4.0.5 | d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e |

## Remaining gates

TASK-009/010 are **incomplete**, despite the implemented local foundation. Real-data quality
policy, bootstrap review and resource calibration remain on hold. No official archive was
ingested in this pass and no synthetic fixture was relabeled as production evidence. The new
CSV CLI's complete all-category run still requires the approved Ubuntu environment and config.
The current production loader uses one fixed dataset asset; real-data partition sizing,
download/preparation performance and production browser certification remain open.

Actual GitHub workflow execution, Pages environment/repository controls, deployment approval,
publication/uncertain-deploy reconciliation and hosted recovery have not been exercised. The
revision checks run before artifact upload and again in the deploy job after environment approval;
the latter uses a read-only Git ref lookup. Their hosted behavior remains unverified.
Thirty-day reliability, independent Reviewer approval, TASK-019 AC-019-8 and milestone/handbook
closure remain open. No commit, push, merge, release tag or deployment occurred.

Implementation file hashes: [hash manifest](task-009-010-2026-09-12-hashes.json).
