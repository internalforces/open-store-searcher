# PR #21 review remediation — 2026-09-13

Reviewed head: `759db3ae3fbc4cccfe976df9485fb605ba86effb`.
Scope: user-requested review assessment, bounded corrections, commit and push to the existing
PR branch. TASK-008 stays active; TASK-009/010 and production release gates remain incomplete.
This is an author assessment, not independent approval. No GitHub messages, thread resolutions,
merge, deployment, dependency, source/status contract or account-setting changes are included.

## Findings and disposition

| Review | Assessment | Change and remaining limitation |
| --- | --- | --- |
| [3996368065](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3996368065), P1 hosting size | Valid; mitigated, production design unresolved | Enforce 1,000,000,000 logical bytes for the entire site. Reject oversized descriptors before asset reads, include Vite output and state metadata, verify final bytes, and promote only a complete temporary candidate. The measured 2,439,358,850-byte snapshot remains undeployable. A guard is not a smaller delivery format. |
| [3996368070](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3996368070), P2 attribution | Valid; corrected | Both reference and bounded serializers use the whole licensing portal, matching the existing publication loader. Preserve each record's category URL and original category-specific freshness evidence. |
| [3996368076](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3996368076), P1 date language | Valid; corrected | Reload and footer instructions branch on the retained dataset's coverage kind. Collection mode names the collection date; verified/synthetic/unavailable behavior and source-coverage caveats remain. |

GitHub's [official Pages limits](https://docs.github.com/en/pages/getting-started-with-github-pages/github-pages-limits)
state a maximum published-site size of 1 GB. The decimal byte bound is deliberately conservative.
Partitioning alone would not reduce total hosted size; compression/compact projection or another
static delivery design must also meet browser and complete-site measurements. Those concrete
architecture/public-interface decisions require approval under AGENTS.md. Publication stays disabled.
Historical full-source dataset hashes belong to their original commits; corrected source metadata
will change serialization hashes. No fresh actual-source replay or production feasibility claim.

## Regression evidence

| Requirement | Exact test evidence |
| --- | --- |
| FR-13 oversized descriptor fails before reading assets or producing a site | `rejects oversized Pages descriptors before reading assets or creating a site` |
| FR-13 combined site assets/state exceed the budget and preserve prior output | `rejects combined Pages assets and staging bytes and removes only its candidate` |
| Inclusive total-size boundary and nested byte accounting | `accepts total Pages bytes %i`; `rejects oversized or invalid total Pages bytes %s`; `counts nested site assets and state metadata in the Pages budget` |
| Whole-archive attribution, exact category URLs and descriptor hashes | `binds exact dataset bytes and matching baseline without asserting source data date`; `matches complete legacy release and metrics with batch size %i` |
| FR-08/14 collection-date wording through search, pending reload and failure | `keeps collection-date instructions through search and failed reload` |
| Verified-date wording preserved | `keeps verified-date instructions when verified coverage is supplied` |
| Actual successful small-site build and tamper rejection | `builds real collection-date assets and rejects tampered staging before building` |

The three reported behaviors were reproduced as failing tests before implementation. Initial
component assertions were corrected to select the page guidance independently of card footers
and use the existing literal date label. Final assertions were reread against the source:
category URLs are checked for all 195 categories; bounded/reference parity catches either serializer
regressing; total-byte tests include nested files, state metadata, the inclusive boundary and a
one-byte excess. A sparse-file Vite stub tests the real builder's post-build guard without allocating
or reading a gigabyte. The unmocked small-site test separately checks actual Vite output.

## Local verification

Runtime: existing cached Node.js 24.19.0 and npm 11.17.0; clean `npm ci` passed with no lockfile
changes or reported vulnerabilities. No global runtime changes.

- Focused Vitest: 140 tests passed across the three affected suites.
- `npm run verify:full`: lint, formatting and TypeScript passed; coverage run reached 678 passing
  tests and two failures (680 total). This command did **not** pass and is not coverage evidence.
- Both failures are the unchanged `unzip-archive.test.ts` native fixture checks. Source/tests
  have no diff from 759db3a; direct Apple `/usr/bin/unzip -O UTF-8 -t` exits 10 because that
  Linux option is unsupported. Collector gates/tests were not relaxed or skipped.
- Separately, build and both search-quality checks passed; `test:e2e:full` passed 68 browser
  checks and `test:a11y` passed 20 checks.
- Final formatter, lint, typecheck, focused tests and `git diff --check` are rerun before delivery.

Current-code Ubuntu full verification is required after the authorized push. At preparation time
it has not run; previous PR CI does not verify these edits. No overall task or release is complete.


## Hosted verification — 2026-09-13

Current implementation `392b52823c3e15d6945925c323c8fb0c631a4d01` passed the approved Ubuntu
`npm run verify:full` in [run 34740089608](https://github.com/internalforces/open-store-searcher/actions/runs/34740089608):
680 tests across 37 files, 68 browser tests and 20 accessibility tests. Global coverage:
93.53% statements, 92.09% branches, 96.51% functions and 95.24% lines; all configured thresholds
passed. This supersedes the pending-hosted-verification state recorded during preparation.
The two native fixture failures are macOS-specific and pass on the approved runner. No test
was skipped or weakened. This follow-up changes only documentation; implementation stays at 392b528.
Production hosting-format/browser feasibility and independent release approval remain open.


## Second review: initial collection-mode loading — 2026-09-13

[Comment 3998845544](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3998845544)
on 96b0838 is valid. The prior App expression only checked the accepted dataset, which is absent
during initial loading and after initial failure. Those states reverted to coverage-date wording.

The internal DisplayLoader now has optional dateBasis metadata. createPublicationLoader declares
collection mode before any request completes. App uses that hint only while no dataset exists;
accepted or retained dataset coverage remains authoritative. EvidenceContext also accepts a date
basis for unavailable coverage, labeling the missing collection date and retaining the source-date
uncertainty caveat without inventing a date, freshness or stale warning. Other loaders keep their
existing behavior. No public dataset/URL contract, status rule, dependency or workflow changes.

| Requirement | Exact regression test |
| --- | --- |
| FR-08 initial loading, first failure, retry and accepted-data collection wording across header, reload help and footer | `labels collection dates before initial load, after failure, during retry and after success` |
| Loader replacement clears collection hints and restores unknown-basis wording | `drops collection-date hints when switching to a loader with unknown date basis` |

Both tests failed before the correction; all 119 component tests across 11 files pass afterward.
The tests use createPublicationLoader itself with deferred fetch responses, ensuring that its
metadata is actually wired to App. The original retained-data/reload regression and verified-date
regression continue to pass. Assertions explicitly check the unknown-date label before success
and the concrete collection date only after a validated response. No network service is contacted.

Pinned local verify:full passes lint/format/typecheck and 680 tests but fails the same two unchanged
Apple unzip native fixtures (682 total). This is not a passing coverage run. Current-code Ubuntu
verification follows the authorized push. Actual-data hosting/browser and release gates stay open.

Second-review local follow-up: build, both search-quality checks, all 68 browser checks and
20 accessibility checks passed. Final formatting and Git whitespace checks passed.


### Second-review Ubuntu verification

Second-review implementation `75356552a53bdf0652c992cd41fb745a7bb55775` passed Ubuntu
`npm run verify:full` in [run 34741144392](https://github.com/internalforces/open-store-searcher/actions/runs/34741144392):
682 tests, 68 browser checks and 20 accessibility checks. Coverage is 93.54% statements,
92.12% branches, 96.51% functions and 95.25% lines; all configured gates passed. This resolves
the second-review verification pending above. Both native unzip fixtures pass on Ubuntu.
The documentation-only follow-up does not change the verified implementation. TASK-008 and
production hosting/browser/release gates remain incomplete.


## Third review: first-deployment bootstrap guard — 2026-09-13

[Comment 3998893054](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3998893054)
on 99e4f94 is valid: stage-refresh selected config.baseline unconditionally in bootstrap mode,
bypassing the current deployment's drift baseline. The CLI now always calls readDeployedBaseline.
Explicit bootstrap requires a reviewed collection baseline and an HTTP 404 from the configured
release descriptor URL before collector invocation. Any existing response, server/authentication
error, redirect or failed probe aborts. Response bodies are cancelled and cancellation failures
propagate. Normal runs retain the same descriptor/hash/metadata/concurrent-change checks and
never bootstrap from a missing baseline file. FR-13 and AC-009 failure preservation are in scope.

The trusted operator must configure the actual deployment URL; a 404 is an absence check, not
proof that the site has never existed. Deployment deletion/recovery and out-of-band publication
remain operator/release concerns under the existing serialized workflow and protection gates.
No workflow, permission, dependency, status mapping or public artifact format was changed.

| Requirement | Exact test evidence |
| --- | --- |
| Reject an initial baseline when current deployed state exists | `rejects bootstrap when a deployed release exists` |
| Permit reviewed first-deployment bootstrap only on explicit absence | `uses the reviewed initial baseline only after an explicit release 404` |
| Do not treat present/error/redirect responses as absence; cancel bodies | `rejects bootstrap on HTTP %s instead of treating it as no deployment` (200, 204, 301, 403, 410, 500) |
| Fail on network errors and response cleanup failures | `rejects bootstrap on a failed deployment probe`; `rejects bootstrap when the missing response cannot be cancelled` |
| Preserve normal deployed baseline binding | `reads only a baseline bound to an unchanged deployed release without credentials or redirects` and the existing rejection matrix |

Focused RED: eight failures before the guard. Focused GREEN: all 21 tests pass with
`npm test -- --project=pipeline src/pipeline/read-deployed-baseline.test.ts` on Node 24.19.0 /
npm 11.17.0. Assertions check the exact initial baseline, single request, request policy,
response cleanup and fail-closed outcomes. Self-review confirmed CLI baseline resolution
precedes collection and all later validation still receives the selected baseline.
Full local verification results follow below. No independent approval or current Ubuntu run
is claimed by this local correction. Existing production-size/browser/quality gates remain open.


### Third-review local verification

- Pinned `npm run verify:full`: lint, formatting and TypeScript passed; 690 tests passed and
  the same two Apple unzip native-fixture tests failed (692 total). This is not a passing
  full/coverage run. Both native source/test files remain unchanged from 99e4f94.
- Separately, `npm run build`, `npm run quality:search:check`, `npm run test:e2e:full` and
  `npm run test:a11y` all exited 0; accessibility passed 20 checks.
- Offline CLI experiment: invoked the actual stage-refresh script with --bootstrap, temporary
  fixture config and a Node preload returning HTTP 200 from fetch. It exited nonzero with
  the bootstrap rejection before collection and created no candidate directory. Temporary
  fixtures were removed; no external service or source archive was accessed.
- Final focused test, formatter and Git whitespace checks passed. Changes remain local and
  uncommitted; no push, GitHub message, thread resolution, merge or deployment was performed.
- Current-code Ubuntu full verification and independent release review remain outstanding.


## Fourth review: deployed descriptor and measurement binding — 2026-09-13

Both comments on a54a499 are valid:
[3998971153](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3998971153)
accepted a baseline-only descriptor, and
[3998971154](https://github.com/internalforces/open-store-searcher/pull/21#discussion_r3998971154)
measured unchecked dataset bytes under observation metadata from potentially another run.

The deployed reader now requires exactly dataset.json and baseline.json, each once, with a
positive safe integer byte length and SHA-256. Invalid entries fail before any baseline fetch.
Entry order is immaterial. The normal baseline bytes/hash, metadata and descriptor reread checks
remain unchanged. This validates descriptor completeness; it does not download the production
dataset again or claim hosted recovery verification. FR-13 / AC-009-5 remain the governing gates.

The research CLI streams dataset.json before creating Vite or launching Chromium, checks the
observed byte length and SHA-256, and rejects missing/malformed bindings and mismatched bytes.
The report now includes the verified datasetSha256 alongside datasetBytes. This scan is outside
the browser elapsed-time measurement and never buffers the multi-GB dataset in Node. No actual
source/browser measurement was rerun; historical evidence remains unchanged and is not
retroactively authenticated by these tests. Research inputs must remain unchanged during a run.
No dependency, infrastructure, public application interface or status rule changed.

| Requirement | Exact regression evidence |
| --- | --- |
| Complete, valid deployed entry set before using a baseline | `rejects %s in the deployed descriptor before reading a baseline` (13 corruption cases) |
| Valid set accepted independently of entry order | `accepts the exact deployed entry set in either order` |
| Reject mixed/corrupt research bytes before tool startup or report creation | `rejects %s dataset before browser measurement or report creation` (truncated, same-size-modified, foreign, missing, missing-binding, invalid-hash, invalid-size) |
| Bind successful report to checked hash, length and observation | `binds browser report to verified observation dataset bytes` |

The CLI tests invoke the actual script and real temporary files. Only Vite/Chromium are replaced
at their module boundaries; a marker proves they are not started for invalid inputs. The same-size
mutation explicitly preserves UTF-8 byte length, exercising SHA-256 rather than only size. The
successful mocked run checks report contents, not real browser performance. Temporary data is removed.

Focused RED: 20 failures before the fixes. Focused GREEN: 43 tests in two files pass with
`npm test -- --project=pipeline src/pipeline/read-deployed-baseline.test.ts src/pipeline/measure-source-browser.test.ts`.
Pinned Node 24.19.0 / npm 11.17.0 verify:full passes lint/format/types and 712 tests, with two
unchanged Apple unzip failures (714 total). This is not a passing full/coverage run. The prior
commit a54a499 passed Ubuntu [run 34743021815](https://github.com/internalforces/open-store-searcher/actions/runs/34743021815)
with 692 tests, 68 browser checks and 20 accessibility checks; that run does not verify these edits.
Current Ubuntu verification and independent release approval remain outstanding.


Fourth-review final local follow-up: separate build and both search-quality checks exited 0;
full browser checks passed 68/68 and accessibility passed 20/20. Final formatter and Git whitespace
checks passed. No further code edits followed verification. Changes remain local/uncommitted;
no push, GitHub message, review-thread resolution, merge or deployment occurred.


## Fourth-review CI diagnosis — 2026-09-13

At 7a1dd37, Ubuntu [Verify run 34746030961](https://github.com/internalforces/open-store-searcher/actions/runs/34746030961)
passed npm run verify:full: 714 tests, 68 browser checks and 20 accessibility checks. This
supersedes the fourth-review pending Ubuntu verification notes, including the two local Apple
unzip failures; those tests passed on the approved runner. Independent release gates stay open.

[Observe Seoul quality run 34746029824](https://github.com/internalforces/open-store-searcher/actions/runs/34746029824)
failed before source collection in both attempt 1 and the diagnostic rerun (attempt 2).
Both logged UND_ERR_CONNECT_TIMEOUT for file.localdata.go.kr:443 after 10000 ms, followed by
observation-rejected / http_contract_changed / Provider probe request failed. The observation
script, workflow and source probe are unchanged from successful a54a499. This demonstrates a
runner-to-provider connection failure, not a descriptor/hash regression or proof of changed
provider schema. It does not establish a provider-wide outage. No candidate or production policy
was produced. No timeout, workflow, acceptance gate or implementation was changed. Further
observation requires restored connectivity; do not retry indefinitely or waive evidence gates.


## CI observation recovery — 2026-09-13

On the user's explicit request to resolve the failed CI, one further diagnostic rerun used the
unchanged 7a1dd37 commit and existing Ubuntu workflow. Attempt 3 of
[run 34746029824](https://github.com/internalforces/open-store-searcher/actions/runs/34746029824/attempts/3)
passed in 14m37s. PR #21 now reports both observe and verify as pass. No code, timeout,
provider URL, workflow, retry loop, validation threshold or deployment setting was changed.

The local approved probe returned limit 200 and range 206 with a 216,485,056-byte archive;
the third hosted attempt then collected and processed all 195 categories, 2,940,404 rows and
894,291,644 CSV bytes with zero parsing errors. The decoded final report matched its logged
SHA-256 f05984f434ff5553d65e5c22b50bf657e8ec65238be9d7c57c3b144cdd8d3b60.
All 195 entries are complete and their row sum matches the reported total. Archive SHA-256:
edb4be5b859ef0a8eaca0cd2a96f58ac82911d3db59775ecf9417d9beabf87ce.
Dataset: 2,439,752,287 bytes, SHA-256
c66d90193e6045852b50e1555a00255a65c01cffb5384d314d0057e07b48fe19.
Processing took 861,234 ms at 2,257,176 KiB peak Node RSS.

This resolves this failed observation run, not all future provider-network reliability.
Attempts 1/2 remain valid connection-timeout evidence; no permanent outage or code regression
was established. Validation remains review_required and publicationApproved remains false.
Production browser/hosting size, reviewed policy/baseline, independent review and release gates
remain open. No new production data policy, publication or automatic retry behavior was introduced.
