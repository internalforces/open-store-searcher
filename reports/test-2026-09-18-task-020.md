<!--
Purpose:        Record TASK-020 documentation and repository verification evidence
Owner:          Tester / Documenter
Update Trigger: When TASK-020 verification is rerun or its reviewed scope changes
Harness Version: 1.1
-->

# TASK-020 verification — 2026-09-18

Verdict: **PASS** for the TASK-020 documentation scope.

## Documentation checks

- Repository formatting passed for 198 supported files.
- Biome lint exited 0 with five existing informational template-literal suggestions in unchanged
  research/script files.
- TypeScript type checking and the Vite production build passed.
- All 41 local Markdown targets in the ten reviewed Markdown files resolved.
- The bug-report, feature-request, and issue-configuration YAML files parsed successfully.
- Git whitespace checks passed.
- A fresh authenticated repository read confirmed private vulnerability reporting is disabled,
  supporting the public SECURITY instructions without changing the setting.

## Full verification

`npm run verify:full` passed in the retained Ubuntu 24.04 container using Node.js 24.19.0,
npm 11.17.0, and Debian Info-ZIP 6.00:

- 797 Vitest tests passed across 44 files.
- Coverage passed: 92.72% statements, 91.09% branches, 95.36% functions, and 94.44% lines;
  the status mapper remained at 100%.
- Synthetic search quality remained 28/30 and reviewed source quality remained 98/100, with no
  safety failures.
- All 68 Playwright browser checks passed across Chromium, Firefox, WebKit, and mobile Chromium.
- All 20 accessibility tests passed across Chromium and mobile Chromium.

The first macOS invocation stopped during coverage with the known Apple Info-ZIP incompatibility
in two tests and an additional Node-version assertion because the shell used Node 22.22.3. The
bundled Node 24.19.0 runtime resolved the runtime assertion. The Ubuntu run resolved the two
platform-specific archive tests without changing or skipping them and is the accepted result.

The isolated verification copy excluded `handbook/ko/**`, `.git`, `node_modules`, and `dist`.
macOS AppleDouble transfer artifacts were removed only from that temporary copy before `npm ci`.
No source, test, dependency, workflow, configuration, publication, or handbook file changed as a
result of verification.

Independent review: [TASK-020 documentation review](review-2026-09-18-task-020.md), Approved.


## Authorized delivery verification — 2026-09-19

Before commit and PR delivery, reran `npm run verify:full` in an isolated copy of the
current checkout in the retained Ubuntu 24.04 container with Node.js 24.19.0 and npm 11.17.0.
The command exited 0: 797 Vitest tests across 44 files, 68 browser checks, and 20 accessibility
tests passed. Existing installed dependencies were reused; no dependency or product file changed.
The copy excluded the Korean handbook and Git metadata. The execution log is retained locally
at `/work/task020-delivery-20260919-verify.log` inside the verification container.

Fresh local checks also resolved 16 inline Markdown file targets across ten reviewed documents,
parsed all three issue-template YAML files, and passed Git whitespace checks. This narrower
inline-link check does not replace or expand the earlier 41-target review.
After the full run, appended delivery evidence/session records and removed redundant trailing
blank lines from eight newly staged documents/templates; staged whitespace checks then passed.
Production, calibration, hosted recovery, and release gates remain open.
