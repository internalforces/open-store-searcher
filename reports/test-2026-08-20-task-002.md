<!--
Purpose:        Independent Tester evidence for TASK-002 repository foundation
Owner:          Tester
Update Trigger: When TASK-002 implementation or verification evidence changes
Harness Version: 1.1
-->

# TASK-002 Independent Test Report

_Date: 2026-08-20_

_Tester scope: independent clean-install, acceptance-command, artifact, dependency, and changed-path verification against `origin/main...HEAD`._

## Conclusion

**VERDICT: PASS**

All required Task 7 Steps 1–4 checks passed with the required pinned runtime. The worktree was clean before this report was created; afterward, this report is the only working-tree change.

## Environment and Clean Install

Every fresh shell declared:

```text
TASK_NODE=/Users/sonmyeong-gwan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node
TASK_NODE_DIR=/Users/sonmyeong-gwan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin
TASK_NPX=/opt/homebrew/lib/node_modules/npm/bin/npx-cli.js
```

| Command | Salient output | Result |
|---|---|---|
| `"$TASK_NODE" --version` | `v24.19.0` | PASS |
| `PATH="$TASK_NODE_DIR:$PATH" "$TASK_NODE" "$TASK_NPX" --yes npm@11.17.0 --version` | `11.17.0` | PASS |
| `PATH="$TASK_NODE_DIR:$PATH" "$TASK_NODE" "$TASK_NPX" --yes npm@11.17.0 ci` | Added 93 packages; audited 94 packages; zero vulnerabilities | PASS |
| `git diff --quiet -- package.json package-lock.json` before and after `npm ci` | Exit status `0` both times | PASS |

`npm ci` emitted a non-failing npm `allow-scripts` notice for `fsevents@2.3.3`, an optional package with an install script not covered by `allowScripts`. It did not change the lockfile or direct dependencies, and npm completed with exit status `0`.

## Required Acceptance Commands

| Command | Salient output | Result |
|---|---|---|
| `npm run lint` through pinned npm | `Checked 8 files`; no fixes | PASS |
| `npm run format:check` through pinned npm | `Checked 7 files`; no fixes | PASS |
| `npm run typecheck` through pinned npm | `tsc --noEmit` completed | PASS |
| `npm run build` through pinned npm | Vite 8.2.1 built 7 modules; emitted a 0.33 kB HTML entry and 11.55 kB JavaScript asset | PASS |
| `npm run verify` through pinned npm | Lint, formatting, typecheck, and Vite build all completed | PASS |
| `"$TASK_NODE" scripts/report-dependency-licenses.mjs` | `Recorded 143 unique package versions.` | PASS |

## Subpath and Build-Artifact Audit

| Check | Evidence | Result |
|---|---|---|
| `npm run build -- --base=/open-store-searcher/` through pinned npm | Successful Vite build | PASS |
| `rg -n '/open-store-searcher/assets/' dist/index.html` | The generated module script used `/open-store-searcher/assets/index-CfUx59hl.js` | PASS |
| `rg -n 'src="/assets/\|href="/assets/' dist/index.html` | No match; `rg` exit status `1` is the expected absence result | PASS |
| Default `npm run build` through pinned npm | Successful; restored the normal relative-base build | PASS |

## Dependency and License Audit

`npm ls --depth=0` reported exactly the approved roots:

```text
@biomejs/biome@2.5.9
@preact/preset-vite@2.10.6
preact@10.29.8
typescript@7.0.2
vite@8.2.1
```

`package.json` and the lockfile root agree on the same exact five direct dependencies. The package metadata is private ESM, MIT-licensed, and pins Node.js 24.19.0 and npm 11.17.0. An independent comparison of `package-lock.json` with `reports/dependency-licenses-2026-08-20.md` found 143 lock package/version pairs and 143 report rows, with zero missing rows, zero extra rows, and zero blank licenses. The generator completed without report drift.

The source PRD contained two MIT project-license statements and no stale Apache project-license statement. ADR-007 records the MIT decision and the Biome license option. The repository-wide stale-license pattern produced two matches only inside the implementation plan's literal audit command; neither match asserts Apache-2.0 as the project license.

## Changed-Path and Scope Audit

`git diff --name-only origin/main...HEAD -- . ':(exclude)handbook/ko/**'` listed the approved foundation configuration, minimal Preact entry, module placeholders, license-report generator/report, and related task/documentation records. No changed path was a handbook, environment/secret/key, production-data, Actions/workflow, deployment, or runtime-server file.

The executable implementation contains only the minimal Preact render path and the license-report generator. The `src/domain`, `src/pipeline`, `src/search`, and `src/shared` directories contain only `.gitkeep`; no search, data schema, status mapping, test harness, tracking, analytics, server, database, paid API, or deployment behavior was added. The application entry uses semantic `main` and `h1` elements.

`git diff --check` completed with exit status `0`. `git status --short` was empty before this report was created.

## Diagnostic Note

One non-gating diagnostic command attempted `find` for a source-directory listing and returned exit status `127` because `find` was unavailable in the shell path. The audit was immediately rerun with the required `rg --files` approach, which confirmed only the two `src/app` source files and `.gitkeep` placeholders. No required acceptance command or verification gate failed.
