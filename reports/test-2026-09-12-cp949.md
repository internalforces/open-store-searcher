# Strict CP949 decoder verification

The user approved build-only iconv-lite 0.7.3 (MIT) and its locked safer-buffer 2.1.2
(MIT) dependency on 2026-09-12. The source contract's `euc-kr` label remains unchanged.
Both header inspection and complete CSV ingestion use CP949 decoding followed by exact
byte re-encoding comparison; malformed or lossy sequences are rejected. UTF-8 remains fatal.

The extension regression failed before the fix: bytes `81 41` became U+0081 and ASCII A
instead of U+AC02. The same test passes after the fix at 909bb9b.

| Requirement | Exact test | Evidence |
| --- | --- | --- |
| FR-08, complete source preservation | `preserves CP949 extension Hangul in quoted multiline fields` | Red before fix, green after fix |
| FR-08, byte bounds | `decodes CP949 extension and ordinary Hangul without consuming bytes outside the view` | Passed |
| FR-08, invalid input rejection | `rejects malformed or truncated CP949 bytes %j` | Four cases passed |
| FR-08, UTF-8 compatibility | `keeps strict UTF-8 decoding and BOM handling` | Passed |
| FR-08, schema evidence | `preserves the source encoding label and CP949 header evidence` | Passed |

Local pinned Node 24.19.0/npm 11.17.0 verification passed: lint, formatting, type checking,
644 tests (three native Info-ZIP checks skipped on Windows), coverage thresholds, build,
synthetic and source search-quality checks. Focused decoder/parser/header tests: 24 passed.
`npm audit --omit=optional` reports zero vulnerabilities. A Vite in-memory build inspected
29 emitted browser module identities: no iconv-lite, safer-buffer or decoder module was present.
The client JS remains 43.83 kB (16.57 kB gzip); this is the existing demo build, not production
dataset performance evidence. No new runtime/browser dependency was introduced.

Hosted Ubuntu full verification passed at 909bb9b in run 34692671364: 647 Vitest tests,
68 browser tests and 20 accessibility tests. Coverage: statements 93.01%, branches 92.18%,
functions 96.43%, lines 95.04%. This is author verification, not independent approval.

Actual observation run 34692668848 passed collection and decoded 127 categories, 1,723,957
rows and 516,036,808 uncompressed bytes without a parser error before exhausting the 6144 MiB
JavaScript heap. The largest observed member contains 945,587 rows and 280,356,442 bytes.
This proves the current whole-candidate row-retention approach is unsuitable for the observed
resource budget. It does not prove that every remaining member decodes successfully.

The research-only follow-up at c3c474a retains counts and hashes across categories, releasing
source rows after each member. Its version-2 `parser-inventory-observation` explicitly carries
`validation: null` and does not claim transformation, validated quality metrics or a baseline.
No production validator requirement was removed or changed. Run 34692888618 and its single
retry both failed before collection with UND_ERR_CONNECT_TIMEOUT (10 seconds). The memory
remediation has lint/syntax checks but still needs successful full-source execution. Complete
parser inventory, full-candidate memory remediation, quality policy and baseline remain open.

The final research-script revision c3c474a also passed the full hosted suite in run 34692891236.
This verifies existing product behavior and static checks; it does not substitute for the failed
external-source replay.
