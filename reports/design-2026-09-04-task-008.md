<!--
Purpose:        Record TASK-008 design evidence and remaining approval gates
Owner:          Architect / Planner
Update Trigger: When the design assessment or verification status changes
Harness Version: 1.1
-->

# TASK-008 Design Assessment

_Date: 2026-09-04_

_Result: Proposed design prepared; implementation and independent review pending_

## Scope reviewed

Loaded AGENTS.md, current memory/task context, Planner/Architect/Implementer prompts, workflow,
standards, traceability, and accepted source/transformation/status contracts. Inspected collector
and transformer code and the calendar validator. No Korean handbook content was loaded.

The user requested TASK-008 execution. It is now the only active task. Existing uncommitted
TASK-008 planning notes in memory/session.md were preserved as historical context.

## Findings and design response

| Finding | Response |
|---|---|
| Collector's common ZIP date is not a verified coverage date | Proposed explicit archive-bound coverage evidence; missing evidence prevents acceptance |
| Official restaurant and alias pages retain daily D-2 metadata | Recorded dated source links in the design; did not generalize this two-page check to all categories |
| Header/identity/status behavior already exists | Reuse the V2 transformer and accepted source checks; keep ADR-013 unchanged |
| Category files can be empty or unread without rows revealing which | Require complete per-category ingestion evidence before candidate acceptance |
| Production normal distributions and JSON budget are absent | Require reviewed policy and baseline; no arbitrary numeric defaults |
| Public JSON and CSV row parser are not implemented | Keep validation staged/internal and reserve exact byte/schema binding and production integration for TASK-009 |
| Source PRD is unavailable at its recorded path | Asked for current path/link; use traceability without claiming exact PRD acceptance |

## Reviewable deliverables

- `docs/superpowers/specs/2026-09-04-task-008-validation-design.md`: API boundaries, provenance,
  missing/changed data policies, freshness convention, first-run handling, JSON limits, and
  V01–V14 requirements/test matrix.
- Proposed ADR-014 in memory/decisions.md, active task acceptance criteria, backlog promotion,
  project status, traceability, and unresolved-evidence records.

## Verification and limits

Documentation-only changes. No code/test/dependency/workflow/public-data change was made.
Executable TASK-008 verification and `npm run verify:full` are not claimed. Source PRD access,
human design approval, implementation/tests, complete production evidence, and independent
Reviewer approval remain outstanding. This assessment is not an independent final review.

The design was checked against the existing interfaces and ownership boundaries. Final
`git diff --check` and local document-link checks are recorded in memory/session.md.
