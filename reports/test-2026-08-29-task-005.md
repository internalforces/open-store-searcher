<!--
Purpose:        Record final local TASK-005 implementation and verification evidence
Owner:          Tester
Update Trigger: When TASK-005 verification or final review status changes
Harness Version: 1.1
-->

# TASK-005 Test Report

_Date: 2026-08-30_

## Result

**PASS for implementation and local verification; independent Reviewer approval pending.** All
automated, live-source, schema-contract, scope, browser, and accessibility gates owned by the
Implementer and Tester pass. This report does not provide the role-separated final approval.

## Pinned Runtime Verification

| Command | Result |
|---|---|
| `node --version` | PASS — `v24.19.0` |
| `npm --version` | PASS — `11.17.0` |
| `npm run test:pipeline` | PASS — 10 files, 74 tests |
| `npm run verify:full` | PASS |
| `git diff --check` | PASS |

The full coverage run passed 75 tests across 11 files at 85.94% statements, 84.22% branches,
89.53% functions, and 89.10% lines. The browser matrix passed Chromium, Firefox, WebKit, and mobile
Chromium. Both desktop and mobile Chromium accessibility scans reported zero automatically
detectable WCAG 2.1 A or AA violations.

The PR #6 remediation adds regression coverage for stalled downloads, already-aborted archive
processes, repository-root staging isolation, impossible ZIP dates, and structured provider
freshness evidence. `reports/review-2026-08-29-task-005-pr-6-feedback.md` records the finding-level
technical disposition.

The second PR #6 remediation adds contract-discovery calendar validation, fail-early Linux Unicode
Info-ZIP capability gating, typed malformed-redirect rejection, and bounded one-byte range-body
streaming. The compatibility check now occurs before any provider request or full transfer.

The third PR #6 remediation adds exact repository ancestry checks, inactivity cancellation while
waiting for response headers, fail-early manual-probe orchestration, explicit rejection of the
unapproved Docker option, and complete handling of short filesystem writes.

## Collector and Contract Verification

- The literal source alias was first covered by a failing test, then restricted to audited
  file-data ID `15045011`.
- Aliases to IDs absent from permission evidence and duplicate mappings remain rejected.
- The 2026-08-29 official archive passed HTTP, byte-count, SHA-256, ZIP integrity, 195-entry,
  timestamp, encoding, delimiter, header, and permission gates.
- The generated contract contains 195 unique filenames and 195 unique audited file-data IDs.
- All 195 headers decode strictly as EUC-KR; header discovery decodes no bytes beyond the complete
  first record.
- The Biome-formatted committed contract SHA-256 is
  `7cd8ce23032b491a4d9859c2cf50d7fa7505ea94c63523cb44bcb5e4242388ec`.
- Reapplying the committed contract to the staged archive returned schema-manifest SHA-256
  `6254a2b3d6626c41f625bb98d79399bdd40318632ce0d54561a997fdd039ceb6` and provider-local entry
  date 2026-08-29.

## Scope Audit

No source record, complete provider archive, workflow, deployment, publication artifact, status
mapping, data as-of value, secret, `.env`, or handbook path is committed. The browser build still
contains seven modules and 11.55 kB of JavaScript; pipeline code and `@types/node` do not enter it.
Temporary archives, candidate files, the Ubuntu verification container, and its image were removed
after verification.

## Remaining Gate

An eligible Reviewer who did not author this implementation must issue `Approved` or
`Request Changes`. TASK-005 remains active until that decision is recorded.
