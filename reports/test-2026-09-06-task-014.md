<!--
Purpose:        Bind TASK-014 UI acceptance criteria to executed verification
Owner:          Tester
Update Trigger: Implementation or verification evidence changes
Harness Version: 1.1
-->

# TASK-014 Verification — 2026-09-06

## Scope and environment

Accepted design: docs/superpowers/specs/2026-09-06-task-014-ui-design.md.
Branch: codex/task-014-search-ui, reused .worktrees/task013-quality.
Base: merged main 9160d570c35d161da9e64688db21c2520f6efaae, verified byte-identical
to reviewed TASK-013 head 5b9b2d5. Original dirty TASK-008 checkout is preserved.
Node v24.19.0 / npm 11.17.0 via PATH=/tmp/open-store-task013-runtime:$PATH.
No packages, lockfile, search/domain/pipeline implementations or workflow changed.

## Executed checks

| Command | Result |
|---|---|
| Baseline npm run test:component | PASS, existing 1 test |
| Card RED npm run test:component | Expected missing ResultCard module failure |
| App RED npm run test:component | Expected 12 failed new interactions against title-only App |
| Layout RED Playwright chromium / wraps results | Expected failure: submit target below 44px |
| npm run test:component after implementation | PASS, 24 tests |
| npm run typecheck | PASS |
| Focused Chromium search/a11y | PASS, 7 tests |
| npm run verify:full | PASS, exit 0; 478 Vitest tests, 20 browser tests, 6 a11y scans |
| git diff --check | PASS |

The full gate includes lint, format, types, coverage, build, both quality corpora,
Chromium/Firefox/WebKit/mobile Chromium search and smoke tests, and desktop/mobile axe.
All six initial/primary-conflict/ambiguous-tie axe scans reported zero violations.
Full log: /tmp/task014-verify-full.log. Global coverage: statements 92.72%, branches
92.08%, functions 96.26%, lines 94.97%. HTML coverage/app/index.html reports 100% in
all four measures; full-covered modules are omitted from the console detail table.
The existing mapper retains its required 100% threshold.

Synthetic quality remains 28/30; source quality remains 98/100; safety failures remain zero.
These are unchanged bounded quality criteria, not a production release conclusion.
Built HTML/CSS/JS reported 0.40/4.53/31.53 kB uncompressed (about 36.5 kB total).
This small synthetic build does not establish full-data latency or production performance.

## Requirement-to-test matrix

| Criterion | Exact generated test evidence |
|---|---|
| U01 / FR-04/05: four statuses and raw pairs | `renders raw pair %s/%s with the approved status %s`; `renders all four demo statuses through actual search submissions` |
| U01: inert source text | `renders HTML-shaped evidence as inert literal text` |
| U02 / FR-06: originals and all lifecycle values | `preserves both addresses, business types and every lifecycle field separately from coverage` |
| U02: missing fields | `keeps unavailable coverage unknown despite a recent row timestamp and marks missing fields` |
| U03 / FR-08: coverage provenance | The raw-pair parameterization asserts synthetic notices; lifecycle test asserts verified coverage separately; unavailable test rejects row-date substitution |
| U04 / FR-09: initial source/disclaimer | `shows truthful demo context, source and disclaimer before searching` |
| U04: per-card source/date | `renders the exact primary once and keeps the same-name address conflict in similar candidates` |
| U04: source URL handling | `does not turn unsafe source URL %s into a link`; `links an HTTPS source without rewriting it and keeps confidence separate from status` |
| U05 / FR-07: primary/conflict | `renders the exact primary once and keeps the same-name address conflict in similar candidates` |
| U05: licensing tie | `does not promote a licensing tie to a primary card` |
| U05: low/medium matching | `keeps name-only matches in similar candidates without changing administrative status`; `shows medium-confidence candidates without inventing a primary match` |
| U05: empty result | `shows non-closure empty guidance and removes previous result cards` |
| U06 / FR-11: responsive keyboard flow | `searches the real form with keyboard and wraps results at 320 CSS pixels` across four browser projects |
| U06: accessible result states | `has no WCAG violations with primary and conflict results`; `has no WCAG violations with ambiguous licensing tie results` |
| U07: example/submit | `fills the example without submitting and supports Enter submission` |
| U07: invalid input | `clears obsolete results on invalid submission %j and associates the error with input` for empty, one-character and whitespace input |
| U07: draft/result separation | `keeps submitted context distinct from the draft and replaces results after another search` |
| U07: dataset replacement | `clears results when supplied dataset changes and displays unavailable coverage honestly` |
| U08: actual query privacy | `keeps real form queries out of browser network, storage, logs and URL` |

The form privacy test self-checks fetch, beacon, XHR, WebSocket, EventSource, local/session
storage and console sentinels before clearing counters and exercising the actual form.
It also observes network requests, URL, storage properties and cookies. Existing standalone
engine sentinels remain untouched. No external service is contacted by tests.

## Visual inspection and corrections

Inspected all three saved Chromium captures: reports/task014-ui/desktop-initial.png,
desktop-results.png and mobile-results.png. Text and badges wrap; raw evidence remains visible;
mobile uses one evidence column and stacked input/button. Initial desktop viewport 1280x1000;
mobile 390x844. Automated wrapping also covers 320px and long submitted query text.

The first screenshot attempt used a preview server without the build's Pages base and produced
an empty page. Restarting the local preview with --base=/open-store-searcher/ fixed this test
setup issue; screenshots were recaptured. The existing Pages-subpath full gate already passed.
One initial component assumption treated district-only evidence as medium; inspected the
unchanged engine contract and corrected the fixture to address-only exact evidence.
TypeScript NodeNext required the named userEvent export and a CSS module declaration.
Biome required a native search landmark around the form. No search policy changed.

## Limits

The default dataset is wholly synthetic and visibly labeled at page and card level.
Production source-cut, loading, serialization and publication remain TASK-008/009/010.
TASK-015 retains richer failure/stale/low-confidence recovery, TASK-016 map links,
TASK-017 fuller manual screen-reader behavior, TASK-018 full-data performance.
Current similarly matched records are rendered from the engine list; production list-size
and partitioning budgets must be resolved before full-data integration. No milestone closes.
