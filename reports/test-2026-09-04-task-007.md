# TASK-007 Verification

Date: 2026-09-04. Requirements: FR-04, FR-07, Section 12.2. ADR-013 accepted by the user.

## Environment and Test-First Evidence

Windows, Node.js 24.19.0, npm 11.17.0, existing locked dependencies. A temporary official
Windows Node distribution was checked against the provider's SHA-256 manifest before use.
Repository dependencies and runtime pins were unchanged.

Before implementation, the domain test could not load the absent mapper and the transformer
suite failed 16 assertions (27 passed): processed status was absent and schema remained V1.
After implementation, focused commands and full verification exited successfully.

## Requirement Evidence

| Requirement | Exact test or evidence |
|---|---|
| Three exact verified pairs and the mixed fourth bucket | `maps the approved pair %s / %s` |
| Missing, partial, unknown, contradictory pairs | `fails safely for aggregate combination %j / %j` (77 combinations) |
| Whitespace, NFD, fullwidth digits and slash, invisible variants | `rejects whitespace and Unicode variants of %s / %s`; `does not read or mutate detailed evidence` |
| No detailed-status inference or source mutation | `does not read or mutate detailed evidence`; `preserves raw evidence while mapping %s / %s` (13 aggregate cases, each with four detailed pairs) |
| Raw values survive internal serialization | `preserves raw evidence while mapping %s / %s` |
| Schema V2, stable identity/search/display/provenance/order | `versions processed output without changing identity, normalization, or ordering`; existing golden identity vectors |
| Empty input never becomes closed | `does not infer a status for an empty stage` |
| File-level coverage enforcement | Exact mapper path in `vitest.config.ts`; `coverage/domain/index.html` |

## Passing Commands

| Command | Result |
|---|---|
| `npm run test:unit` | 86 passed, no skips |
| `npm run test:pipeline` | 131 passed; two existing Windows Info-ZIP integration skips |
| `npm run typecheck` | PASS |
| `npm run verify:full` | Exit 0: lint, format, typecheck, coverage, build, full browser matrix, accessibility |
| Coverage suite within full verification | 13 files passed; 218 tests passed, two existing skips |
| Full Pages-subpath smoke matrix | Chromium, Firefox, WebKit, mobile Chromium: 4 passed |
| Accessibility matrix | Desktop/mobile Chromium: 2 passed, zero detected WCAG A/AA violations |
| `npm run format:check` and `git diff --check` after documentation updates | PASS; new task files also checked for trailing whitespace |

| File | Statements | Branches | Functions | Lines |
|---|---|---|---|---|
| `src/domain/map-license-status.ts` | 100% (7/7) | 100% (12/12) | 100% (1/1) | 100% (4/4) |
| All files | 84.20% | 82.37% | 89.14% | 87.17% |

The global thresholds remain 80/75/80/80 (statements/branches/functions/lines).
The mapper alone has the required 100% file threshold in all four dimensions.
The two skipped tests predate this task and require the approved Linux Info-ZIP environment;
all TASK-007 tests executed. Existing UI smoke and axe tests validate the current application
foundation, not future result-card status UI. Production distributions, `dataAsOf`, publication,
deployment, and milestone closure remain outside this task.
