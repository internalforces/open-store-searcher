# PR #14 review remediation — 2026-09-05

Scope: TASK-013, FR-03/FR-07, Q07/Q08 provenance and fixed-corpus quality checks.
User authorized review implementation, commit and push. Baseline: `379a8503c91f8c3f0ed90be0bbd6bca0c39235a9`.

## Findings and regression evidence

| Finding | Resolution | Exact regression test |
|---|---|---|
| R1 / discussion_r3940688875 | Preserve the full parenthesized legal-locality address before punctuation removal when the prefix is an address-only core. Keep bare trailing business names inferred and district contradictions ambiguous. | `R1 preserves parenthesized localities in address-only queries` |
| R2 / discussion_r3940688877 | Compare actual source bytes against the audit's formatted digest, falling back to raw digest only when no formatted value exists. Reject invalid/missing/mismatched binding with exit 2 and no result before evaluation. | `R2 checks the source audit binding before reporting success: %s` (modified corpus, formatted mismatch, raw mismatch, missing binding, valid raw-only binding, valid formatted binding with different raw hash) |
| R3 / discussion_r3940688879 | Consume complete adjacent floor/unit sequences in component parsing while preserving the original address key and unrelated trailing-number ambiguity. | `R3 consumes adjacent floor and unit notation atomically: %s` (four notation variants) |
| R1/R3 integration | Both cited source records and the independent-review floor-before-locality record return medium-confidence Top-3 matches without a primary selection. | `R1/R3 retrieves the reviewed source records with medium confidence` |

Parser assertions are in `src/search/compare-search-address.test.ts`; source and CLI regressions
are in `src/search/search-quality.test.ts`. CLI mutations run in owned temporary copies and are
cleaned in finally; committed source fixtures, labels and audit bytes are unchanged. The modified
corpus test first proves its numeric check still passes, isolating the missing provenance gate.

## Verification

- Before implementation: focused regression run failed 9 tests, passed 57; all three review
  defects reproduced. Existing valid adjacent-unit notation also remains covered.
- Independent review found a floor-before-locality interaction in a third retained source record.
  Its parser and source integration regressions both failed (2 failed / 66 passed). Shared
  floor/unit stripping now preserves the primary road/lot number before removing detail; both
  spaced and adjacent comma notation are covered. No fixture or label changed.
- Final focused parser/candidate/quality run: 80 tests passed, including both positive audit
  binding cases and the independent-review interaction.
- Pinned Node v24.19.0, npm 11.17.0, ICU 78.3: `npm run verify:full` exited 0 with 455 Vitest
  tests in 20 files, eight browser tests and two zero-violation accessibility scans.
- Full verification includes lint, format, typecheck, coverage, build and both quality checks.
  Coverage: 92.53% statements, 91.74% branches, 96.01% functions, 94.83% lines.
- Fixed synthetic corpus: 28/30 (93.3333%); fixed source corpus: 98/100 (98%); safety failures zero.
  Exact runtime/code/corpus/audit bindings: `quality-2026-09-05-pr14-synthetic.json` and
  `quality-2026-09-05-pr14-source.json`, captured from the successful full run.
- An initial full run caught a strict tuple typing error in a new test; corrected before the
  successful run above. Two pre-existing informational sampler style notices remain non-failing.

Independent Reviewer: Approved after the floor-before-locality follow-up. Reviewer independently
ran 68 parser/quality tests, confirmed the checked source CLI (98/100, safety zero), and passed
whitespace checks; reviewed the final full-run evidence. All three PR findings and the follow-up
are resolved. No dependency, status mapping, source delivery, public interface,
architecture, deployment or milestone change. All production/UI/performance gates remain separate.
