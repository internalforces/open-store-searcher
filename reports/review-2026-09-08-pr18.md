# PR #18 review remediation — 2026-09-08

Scope: TASK-017, FR-16 and PRD Section 14.3. User authorized implementation, commit and push.
Base: 633fbfa4bd19e8bfe29f00899491e0963a72540a, existing codex/task-017-accessibility worktree.

## Findings and resolution

- 3953627026 (P2), accepted: name/address labels collide for separate licensing records.
  SearchResults now supplies a one-based position across all displayed groups, including the
  primary result. ResultCard appends that position to its existing name/address label.
  Identical categories and statuses remain distinguishable without exposing internal IDs.
  Positions identify the current displayed order, not a persistent business identity.
- 3953627033 (P2), accepted: mutually exclusive warning branches suppress similar guidance
  when eligible top matches tie. Independent conditions now emit both applicable warnings.
  Empty-result safety and persistent repeated-submission announcements are preserved.

## Requirement | Evidence

| Requirement | Exact regression evidence |
| --- | --- |
| Distinguish same-name/address candidates, even with identical status/category | `distinguishes identical candidate evidence for 가상동률 식당 서울특별시 종로구 자하문로 20`; `distinguishes identical candidate evidence for 가상동률 식당` |
| Announce tie and similar guidance together, including repeat submission | `announces both tie and similar guidance together on repeated searches` |

Tests are in src/app/accessibility.test.tsx. The first pair checks exact distinct accessible
labels, actual keyboard focus and unchanged status evidence in eligible and similar groups.
The combined-state case runs the real search model with two ties and a conflicting third record,
asserts both counts and warnings in the same persistent status region twice, and checks the
similar candidate has position 3. Existing missing-name assertion includes its new position.

## Verification

Runtime: existing /tmp/open-store-task013-runtime, Node 24.19.0 / npm 11.17.0.

- RED: all three new cases failed against the original implementation for the reported reasons.
- First GREEN attempt exposed jsdom's absent scrollIntoView API during the new keyboard check.
  A per-card test double supplies only that browser API; native scrolling remains covered by
  existing browser tests. No product workaround or global test setup change was added.
- `npm run test:component`: exit 0, 92 passed across 8 files, no unhandled errors.
- `npm run verify:full`: exit 0, 564 Vitest tests, 56 browser tests, 18 accessibility tests
  (22 axe scans, zero violations), lint, format, types, build and search-quality checks passed.
- Coverage: 92.99% statements, 92.41% branches, 96.68% functions, 95.25% lines.
- `git diff --check`: passed.

Implementation and assertion review performed inline: positions follow actual rendered order
across primary/remaining/similar groups; every result card receives a position; conditions do
not suppress overlapping warnings; no classification, dependency or public URL change.
No unresolved finding from the two supplied comments. This is not independent review approval.
No new manual VoiceOver speech observation was performed. Prior assisted observations remain
historical evidence for the earlier revision and do not prove the changed speech verbatim.
No merge, deployment, handbook work, or original dirty checkout modification.
