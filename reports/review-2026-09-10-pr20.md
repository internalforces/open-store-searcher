# PR #20 review remediation — 2026-09-10

All three P2 comments on fa80fd4 were checked against committed evidence and accepted.

| Comment | Resolution | Verification |
|---|---|---|
| 3968540998: FR-12 report location | Replaced checkout-local direction with a relative Markdown link; corrected the same active-task pointer. | Both targets resolve to the committed report without a local worktree. |
| 3968541010: home-directory disclosure | Replaced the absolute checkout path in the public transcript with `<repository>`. | Transcript differs only by that literal replacement; no home-path prefix remains. Earlier Git history and review comments are unchanged. |
| 3968541022: undefined artifact scan | Preserved the original five named regular expressions and decoding/engine/scope in evidence JSON; added an executable reproduction command to the report. | Ran the exact documented command: all six rebuilt file hashes match, zero hits. |

Pinned Node 24.19.0 / npm 11.17.0 `npm run verify:full` exited 0: 598 Vitest tests,
68 browser tests and 20 accessibility tests. All 53 reviewed source hashes remain identical.
Existing 2026-09-09 evidence is historical; this rerun confirms the unchanged application.
Format and Git whitespace checks pass. No product code, dependencies, security policy,
workflow permissions or public interfaces changed. No independent approval is claimed.
TASK-019 review remediation is complete; TASK-008 stays on hold and release gates remain open.


## Second review follow-up

Accepted comment 3979191617 on c268f85: the separate TASK-019 reference in
`memory/known-issues.md` still pointed at the ignored local worktree. Replaced it with
`../reports/security-2026-09-09-task-019.md`, matching the traceability and active-task links.
All three links resolve to the tracked report without relying on a local worktree. Git
whitespace validation passes and all 53 reviewed source hashes remain unchanged. This is a
Markdown-reference correction only; the full 598/68/20 verification above remains applicable
and was not rerun. No product or security-policy change, merge or deployment occurred.


## Third review follow-up — 2026-09-11

Both unresolved P2 comments on 550901f are valid and addressed locally:

| Comment | Correction | Evidence |
|---|---|---|
| 3980150572 / FR-12, Section 14.4 | Added AC-019-1 through AC-019-8 with criterion-level evidence in the [task record](../tasks/completed.md#task-019-acceptance-criteria-and-evidence). The committed history contains no separately approved checklist or Actions waiver; the reconstruction explicitly states that provenance. AC-019-8 stays unchecked, TASK-019 is restored to the backlog as deferred/incomplete, and current task/project status is consistent. | Verified the missing workflow directory, unchecked criterion, backlog entry and local evidence links/anchors. Only the bounded application assessment is complete. |
| 3980150585 / FR-10 | Replaced the three incorrect line references with full-commit GitHub links to map encoding/fixed hosts (14–24), protected tabs (19–30), and source URL filtering/referrer policy (42–49). | Read each range from reviewed commit b614838 and asserted the enforcing statements are present; all three files match the current checkout. |

Validation: all 53 reviewed source hashes still match; all local Markdown targets and anchors
in the changed documents resolve to tracked files; pinned `npm run format:check` and
`git diff --check` pass. This correction changes Markdown only. The recorded full verification
(598 Vitest, 68 browser, 20 accessibility tests) remains historical evidence for the unchanged
application and was not rerun for this correction. No independent approval is claimed.

No application code, dependency, security policy, workflow, deployment or handbook change was
made. No commit, push, GitHub comment, or review-thread resolution was performed in this pass.
