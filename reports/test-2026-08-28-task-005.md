<!--
Purpose:        Record TASK-005 implementation and verification evidence
Owner:          Tester
Update Trigger: When TASK-005 verification or the archive-contract gate changes
Harness Version: 1.1
-->

# TASK-005 Test Report

_Date: 2026-08-28_

## Result

**Implementation verification passed; task closure blocked.** The automated implementation is
green, while the required live schema-contract acceptance remains blocked by ISS-001 and final
independent review has not run.

## Pinned Runtime Verification

| Command | Result |
|---|---|
| `node --version` | PASS — `v24.19.0` |
| `npm --version` | PASS — `11.17.0` |
| `npm run test:pipeline` | PASS — 9 files, 51 tests |
| `npm run verify:full` | PASS — lint, format, typecheck, coverage, build, 4 browser smoke tests, and 2 accessibility tests |
| `git diff --check` | PASS |

Coverage passed at 85.80% statements, 82.99% branches, 88.57% functions, and 88.80% lines.
The browser build contains seven modules and 11.55 kB of JavaScript; pipeline modules and
`@types/node` are absent from the browser output.

## Covered Collector Outcomes

Offline tests cover approved source URLs and headers, permission uniqueness, strict UTF-8/EUC-KR
headers, denial without retry, redirect drift, range drift, partial and oversized transfers,
temporary cleanup, ZIP corruption, unsafe entries, process output limits, missing categories,
permission and schema drift, mixed entry dates, deterministic schema discovery, and changed versus
unchanged digest outcomes.

## Live Probe Evidence

The official probe returned HTTP 200 for the limit endpoint, HTTP 206 and one byte for the range
request, and HTTP 200 with 215,968,197 bytes for the complete archive. SHA-256 was
`1058ca425f1b59d6fb4bdee53d524a99fdc10b32684dd9805013e1c459506dc8`. ZIP integrity and the
195-entry count passed. The temporary archive was deleted after evidence collection.

Both tested macOS Info-ZIP builds transformed UTF-8 Korean filenames, so the exact permission and
schema mapping correctly remained rejected. No schema contract, record value, production dataset,
workflow, publication output, deployment, status mapping, secret, `.env`, or handbook path changed.

## Remaining Gates

1. Generate and verify the schema-only 195-entry contract on a compatible Ubuntu 24.04 Info-ZIP
   environment, or obtain approval for an adapter revision.
2. Run independent final Reviewer approval after that contract is accepted.
