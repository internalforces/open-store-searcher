# TASK-017 verification — 2026-09-08

## Result and scope

Implementation and automated verification pass. Actual VoiceOver speech/navigation observation
remains unavailable through the current tool after an authorized execution attempt; TASK-017 remains active until that manual gate is resolved.
No WCAG conformance or screen-reader signoff is inferred from axe or native accessibility trees.
Approved design: FR-11, baseline FR-16 and PRD Section 14.3. No TASK-023 custom selection model,
source/status mapping, production delivery, dependency, public URL or deployment change.

## Requirements and evidence

| Requirement | Exact test / observation | Result |
|---|---|---|
| Named lists, same-name address identity and preserved evidence | `names candidate lists and distinguishes same-name addresses without changing evidence` | PASS |
| Parcel/missing-address fallback | `uses parcel and missing-address labels for candidate evidence (%s)` | PASS, both branches |
| Explicit results access and repeated invalid correction | `offers explicit results focus while returning valid and invalid submissions to input` | PASS |
| Empty, ambiguous and similar guidance with repeated announcements | `announces safe guidance for %s in the persistent status region` | PASS, three independent queries |
| Pending retry, duplicate guard, focus and completion | `keeps the retry control focusable and ignores repeated activation while pending` | PASS |
| Forward/backward candidate navigation and visible heading | `TASK-017 traverses candidate evidence forwards and backwards with visible keyboard focus` | PASS, four projects |
| Loading/failure/recovery and no focus theft from edited input | `TASK-017 keeps retry focus during pending, failure and recovery without stealing edited input` | PASS, four projects |
| Source/map keyboard order and deliberate source activation | `TASK-017 reaches source and map links in order and activates source only on request` | PASS, four projects; source intercepted locally, no Referer |
| Mobile/tablet/desktop, long text, 320px reflow and 200% text | `TASK-017 reflows at 320px with doubled text and long search content`, same exact template at 768px and 1280px | PASS, four projects |
| Automated error/empty/similar accessibility | `TASK-017 has no WCAG violations in invalid state with enlarged text`, same template for empty and similar | PASS, desktop/mobile |
| Existing safety, privacy, source/maps and recovery behavior | Existing TASK-014/015/016 suites retained in `verify:full` | PASS |

## Commands

Pinned `PATH=/tmp/open-store-task013-runtime:$PATH`, Node 24.19.0 / npm 11.17.0.

- Component RED: seven new tests failed before implementation. Null-address fixture was then
  corrected to the existing string-address contract; this is not a source contract change.
- Tall-card RED: new 320px heading-in-viewport assertion failed before the focus-scroll fix.
- `npm run test:component`: exit 0, 89 tests.
- `npm run verify:full`: exit 0, 561 Vitest tests; 56 browser checks (Chromium, Firefox, WebKit,
  mobile Chromium); 18 accessibility tests / 22 axe scans, zero violations.
- Coverage: statements 92.87%, branches 92.38%, functions 96.35%, lines 95.11%; status mapper 100%.
- Focus-scroll regression: four browser projects passed after the fix.
- Three Chromium reflow cases rerun only to retain visual artifacts after the separate axe run
  cleared Playwright's default output. Exit 0. Screenshots: [320px](task017-visuals/task017-320-text200.png),
  [768px](task017-visuals/task017-768-text200.png), [1280px](task017-visuals/task017-1280-text200.png).
- `git diff --check`: exit 0. Existing informational Biome template suggestions remain non-failing.

The temporary map-fixture edit accidentally replaced the E2E file during preparation. It was
immediately reconstructed from its unchanged HEAD baseline plus this task's exact additions;
all original tests remain, and the final full run above occurred after that correction.

## Manual observations and limitations

Native Chrome local preview, 2026-09-08: searched the synthetic same-name candidates, inspected
the native named content list and address-bearing article accessibility names, and confirmed
input focus after submission. Native Chrome zoom control explicitly showed 200%. Keyboard
results navigation initially scrolled to a tall card's bottom; the reproducing test and guarded
article-focus `scrollIntoView` correction now keep the heading, status and address first in view.
Rechecked that behavior at native 200% zoom; outline and content were visible. Restored native
100% zoom. Inspected retained 768px full-page and 320px long-content crops for wrapping.

This is visual/keyboard/native AX evidence only. No actual VoiceOver speech, announcement timing,
rotor, or other assistive-technology pass is claimed.
User authorization was subsequently granted; the bounded execution attempt
and remaining observation-tool limitation are recorded below.

Manual completion checklist: confirm initial input/help; search same-name candidates and read
list identity/address/status/source/as-of together; repeat the same search; submit invalid and
empty-result queries; check error and safe non-closure announcements; test retry failure/success
and retained-data recovery using the controlled browser fixture; ensure no focus theft or traps.
Record browser/AT version, observations and failures before closing TASK-017.


## Authorized VoiceOver execution attempt — 2026-09-08

The user explicitly authorized temporary VoiceOver execution and commit/push. On macOS 26.6.2,
VoiceOver bundle version 10, the native System Settings accessibility pane reported the
VoiceOver toggle **on**. The VoiceOver process was also running. Chrome's synthetic search
input and Search button were exercised while that toggle was on.

Direct CUA access to `com.apple.VoiceOver` timed out twice, including after checking the caption
panel. VoiceOver Utility reported the caption panel enabled, but the available per-app capture
exposed Chrome and its native AX tree without a readable VoiceOver caption/speech output.
Apple's documented copy/save-last-spoken-phrase commands were attempted; no speech audio
archive was observed. No clipboard contents were read or pasted as evidence. Consequently,
actual spoken names, live announcement timing, rotor behavior and recovery announcements
remain unverified. No manual screen-reader pass is claimed from process state or AX text.

Cleanup: used the native System Settings switch and verified **off**. No AppleScript control,
new permission, downloaded voice, dependency or security change was introduced. The caption
setting was inspected, not manually edited. TASK-017 remains active for direct AT observation;
this limit does not prevent the user's expressly authorized commit/push of reviewed code.

Reference commands were checked against [Apple's VoiceOver general commands](https://support.apple.com/en-euro/guide/voiceover/cpvokys01/10/mac/26)
and [last spoken phrase commands](https://support.apple.com/en-euro/guide/voiceover/vo2725/10/mac/26).
