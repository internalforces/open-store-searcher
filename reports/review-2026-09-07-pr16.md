<!--
Purpose:        Record PR #16 remediation and independent verification
Owner:          Implementer / Reviewer
Update Trigger: Remediation or verification evidence changes
Harness Version: 1.1
-->

# PR #16 review remediation — 2026-09-07

Base reviewed commit: 8dc6de4. User authorized fixes, commit and push to the existing
codex/task-015-recovery-ux branch. Original dirty TASK-008 work is preserved.

| Finding | Resolution and exact evidence |
|---|---|
| R1 / P1: Reject records with blank source labels | Require a nonblank trimmed sourceLabel while preserving original text; `excludes blank record attribution %j and rejects an all-blank payload` (empty, spaces, tab/newline); `preserves nonblank attribution exactly`. FR-07/source-attribution invariant. |
| R2 / P2: Skip preparation after the load is cancelled | Early cancellation guard before prepareDisplayData; `does not read an obsolete payload after %s` for source change and unmount. Tests wait for actual loader start before cancellation and observe zero payload reads. |
| R3 / P2: Record the committed task state | memory/project.md and tasks/active.md now identify initial delivery 8dc6de4 and PR #16. Session handoff distinguishes historical pre-commit work from delivered state. |
| R4 / P2: Avoid rebuilding the search index after every load | Preparation returns a filtered reusable index; App consumes it directly. `builds one real index per load and reuses it for searches` uses a call-through spy and real result assertions; `returns a searchable filtered index with complete exclusion diagnostics` verifies duplicates, display-invalid records, returned results and diagnostics. |

## Verification

- R1 RED: three blank-label failures; GREEN: all four attribution cases passed.
- R2 initial immediate-unmount test did not start the effect and was strengthened before the fix.
  Corrected RED: both source-change/unmount cases failed; GREEN: both passed with zero reads.
- R4 RED: repeated index call count and missing reusable index assertions failed.
- Focused combined GREEN: 76 component tests passed; typecheck passed.
- Pinned Node 24.19.0/npm 11.17.0 `npm run verify:full`: exit 0, 530 Vitest tests,
  28 browser checks, 10 accessibility tests / 14 axe scans with zero violations.
  Global coverage: 92.89/92.33/96.62/95.18 percent. Log: /tmp/pr16-verify-full.log.
- Independent read-only reviewer review_task015: Approved; 38 focused component tests
  and diff checking passed independently. No remaining findings. Source/runtime hashes are
  recorded in reports/pr16-verification-manifest.json.

## Boundaries

No runtime/dependency/status/source/public schema or workflow changes. This is the existing internal
synthetic UI boundary. No production performance or publication claim, no merge or deployment.
