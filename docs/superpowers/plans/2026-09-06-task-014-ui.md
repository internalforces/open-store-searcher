# TASK-014 Search UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** Deliver a synthetic search page with truthful administrative-status evidence.
**Architecture:** Local Preact state submits to the existing generic search engine. Typed
internal records carry original evidence independently of match confidence. Shared context
renders dataset coverage and source consistently on the page and cards.
**Tech Stack:** Preact 10.29.8, TypeScript 7.0.2, Node 24.19.0/npm 11.17.0, existing Vitest/Playwright/axe.
**Spec:** docs/superpowers/specs/2026-09-06-task-014-ui-design.md (Accepted).

## Global Constraints

- Only TASK-014; no dependency, public schema, production source-cut, status mapping or deployment changes.
- Preserve source text. Synthetic provenance and dates must be visibly distinguished.
- No query network/logging/storage or URL state. No handbook access.
- Existing quality checks and coverage thresholds remain mandatory.
- No commit or push requested; deliver uncommitted changes on codex/task-014-search-ui.

### Task 1: Evidence cards (U01–U04)

Files: create src/app/display-data.ts, evidence-context.tsx, result-card.tsx,
result-card.test.tsx. DisplayRecord extends SearchRecord with category, businessType,
rawStatus, dates and sourceLabel. DisplayDataset contains records, coverage and exampleQuery.
Coverage is a discriminated union of synthetic date, verified date, or unavailable.
ResultCard consumes CandidateMatch<DisplayRecord> and Coverage; EvidenceContext renders Coverage.

- [x] Write parameterized tests asserting exact raw pairs and expected four statuses:
  `expect(within(screen.getByRole('article')).getByText(expected)).toBeTruthy()`.
- [x] Run `npm run test:component`; require missing module RED before implementation.
- [x] Implement mapping via `mapLicenseStatusV1(record.rawStatus)` and JSX text nodes,
  render evidence with `<dl><dt>...</dt><dd>{value ?? '제공되지 않음'}</dd></dl>`.
- [x] Run component tests to GREEN; inspect missing and date-state assertions.

### Task 2: Search page (U04/U05/U07)

Files: modify src/app/app.tsx, app.test.tsx; create search-form.tsx,
search-results.tsx, demo-data.ts, app.css. App consumes optional DisplayDataset.
SearchForm consumes value/error/exampleQuery and onChange/onSubmit callbacks.
SearchResults consumes SearchResult<DisplayRecord> and Coverage.

- [x] Add real-engine tests using exact-plus-conflict, licensing ties and missing evidence.
  Submit through `user.type(input, query); user.click(screen.getByRole('button', {name:'검색'}))`.
  Assert no primary for ties/low and prior articles absent after invalid submission.
- [x] Run component tests and inspect expected RED for missing search form.
- [x] Implement local `useState` and `useMemo(() => createSearchIndex(dataset.records), [dataset])`;
  submit with `searchCandidates(index, draft)`, preserving returned group order.
- [x] Add labeled synthetic demo records and CSS wrapping/focus/touch layout.
- [x] Run component tests to GREEN, typecheck and formatting checks.

### Task 3: Browser evidence and delivery (U06/U08)

Files: extend tests/e2e/search.spec.ts and accessibility.spec.ts; preserve engine tests.
Add browser sentinel setup under tests/setup if reuse warrants extraction.

- [x] Before styling implementation, add keyboard form and overflow assertions:
  `expect(await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth)).toBe(true)`.
- [x] After page load, intercept fetch/beacon/constructors/storage/console and self-test
  interception; clear counters, exercise the real form, require zero calls and URL changes.
- [x] Axe-scan initial and populated results at configured desktop/mobile sizes.
- [x] Run `npm run verify:full` with pinned PATH and `git diff --check`.
- [x] Review current source/tests, inspect screenshots, record requirements/test-name evidence
  and limitations in reports/test-2026-09-06-task-014.md and review-2026-09-06-task-014.md.
- [x] Update active/completed, session, decisions, architecture and traceability after gates pass.

Self-review: U01–U08 covered; no new public contract, provider retrieval or runtime loader.

Execution complete: independent review Approved; test and review reports contain evidence.
